name: moto_gps_app
description: "Aplicatie GPS Moto cu inregistrare traseu si export GPX"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^13.0.1
  flutter_background_service: ^5.0.9
  sqflite: ^2.4.1
  path: ^1.9.0
  permission_handler: ^11.3.1
  path_provider: ^2.1.2
  share_plus: ^10.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
